pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Getting project files'
            }
        }

        stage('Run Python Program') {
            steps {
                bat 'python app.py'     // For Windows
                // sh 'python3 app.py'  // For Linux
            }
        }

        stage('Complete') {
            steps {
                echo 'Hospital Appointment Management System executed successfully.'
            }
        }
    }
}